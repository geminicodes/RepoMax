###############################################################################
# ReadyRepo Client (Vite) env example
# Copy to `client/.env.local` for local dev.
#
# IMPORTANT:
# - Vite exposes ONLY variables prefixed with `VITE_` to the browser bundle.
# - These are not secrets. Firebase web config values are meant to be public.
###############################################################################

# API base (default fallback in code is `/api/v1`)
# Examples:
# - Local (with proxy): /api/v1
# - Deployed API: https://api.example.com/api/v1
VITE_API_URL=/api/v1

# Firebase Web App config
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=

# Optional: enables Firebase Analytics if present
VITE_FIREBASE_MEASUREMENT_ID=

